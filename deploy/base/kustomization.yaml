apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: taskmgr

resources:
  - namespace.yaml
  - serviceaccount.yaml

# Story phases (US1, US2) will append Deployment, Service, NetworkPolicy,
# and Job resources to this list.
