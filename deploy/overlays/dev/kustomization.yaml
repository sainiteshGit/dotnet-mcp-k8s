apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: taskmgr
namePrefix: dev-

resources:
  - ../../base

# Story phases will append patches for env-specific config (image tags,
# replicas, resource limits, NetworkPolicy overrides). Left empty for now.
patches: []
