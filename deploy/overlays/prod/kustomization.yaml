apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: taskmgr
namePrefix: prod-

resources:
  - ../../base

# Story phases will append patches enforcing prod-specific constraints
# (e.g., higher replica counts, stricter resource limits, MCP mutations
# disabled — see scripts/lint-prod-overlay.sh). Left empty for now.
patches: []
